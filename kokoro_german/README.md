# German Kokoro Code

This folder is for the optional local German Kokoro-compatible Python package.

Expected layout:

```text
kokoro_german/
  kokoro/
    __init__.py
    ...
```

The fresh checkout does not include that package or the German model weights. German synthesis is available only after those assets are added separately.
